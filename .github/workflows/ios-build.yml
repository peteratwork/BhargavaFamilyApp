name: iOS Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build for iOS Simulator
    runs-on: macos-26

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Show Xcode project information
        run: |
          xcodebuild -version
          xcodebuild -list -project BhargavaFamilyApp.xcodeproj

      - name: Build
        run: |
          xcodebuild \
            -project BhargavaFamilyApp.xcodeproj \
            -target BhargavaFamilyApp \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            -showBuildTimingSummary \
            CODE_SIGNING_ALLOWED=NO \
            build
